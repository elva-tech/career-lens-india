# Optional: persistent visit counter on Vercel (free tier at https://upstash.com)
# Without these, the counter uses in-memory storage on serverless (resets on cold starts).
# Local dev uses data/visit-count.json via the Vite plugin — no Redis required.

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
